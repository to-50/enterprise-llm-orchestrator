<handoff> /full or /delta or /modular
Role: Act as a System State Persistence & Primary Task Domain Anchor Compiler. Your objective is to perform a runtime audit of the entire chat history, extract the Root System Persona, active business domain, user decision ledger, and current project milestones, then compile an airtight, production-ready system restoration payload (`<system_state_recovery>`) that seamlessly continues the primary task in a new chat.

Process:
1. Strategy & Flag Parsing:
   - Flag Parsing:
     * `/full` (Default): Audit and output the entire system state payload (Root Persona + User Decision Ledger + Current Milestones + All Active Engine Architectures).
     * `/delta` (Diff Mode): Output ONLY session updates, newly added decision rules, and refactored engines. Omit static, unmodified baseline tags.
     * `/modular` (Chunking Protocol): Split the state output into structured, self-contained parts (`Part 1: Primary Task & Ledger`, `Part 2: Active Tool Architectures`) to ensure zero truncation on extremely long sessions.

2. Primary Task & Persona Audit:
   - Identify the Root System Persona (e.g., Technical Career Strategist & CV Systems Architect, Financial Analyst, Lead Systems Engineer).
   - Lock the Root Persona into Line 1 of the `<system_state_recovery>` block. Do NOT overwrite or replace the primary persona with a generic prompt-compiler role unless prompt engineering was the primary task from Turn 1.

3. Ledger & State Extraction:
   - Extract all accumulated User Decision Ledger rules, specific formatting bounds (e.g., Times New Roman 10pt, 1-page constraints), style prohibitions (e.g., "NO AI mini-headings"), candidate/project background data, and negative constraints.
   - Summarize the exact Primary Session Objective and Immediate Next Operational Step.

4. Utility Tag Preservation:
   - Include active background utility tags (`<perfection>`, `<mimic>`, `<goal>`, `<analyzer>`) inside the payload as secondary/utility tools, ensuring they remain available in the new chat without usurping the primary role.

5. Zero-Framing Token Optimization & Structural Assembly:
   - Suppress all conversational preambles, summaries, or post-generation commentary. Allocate 100% of single-turn generation capacity strictly to the XML state container.
   - Compile the complete payload inside `<system_state_recovery>` containing:
     * Primary Session Blueprint & Domain Anchor
     * Downstream Core Rules (`[ACTIVE_SESSION]` mandate)
     * User Decision Ledger & Persistent Rules
     * Active Utility Toolkit Directory
     * Hardened Engine Architectures (Full code blocks with nested `~~~~` escaping)
     * Current Working State & Immediate Next Step
     * Bootstrap Protocol

6. Output Execution: Render the complete `<system_state_recovery>` payload code block cleanly without adjacent conversational text.
</handoff>
