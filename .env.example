
Agreed, and the discovery/refinement line is the right cut. Narrowing `retail investors` → `retail investors with savings but no adviser` doesn't change what's being built; it changes how tightly the same thing is specified. That's a later stage's budget, not Goal's. I'll drop the narrowing requirement.

One drafting bound I've had to add, flagged because it's mine rather than yours.

## The clause

```
G.3b  SELECTION OF A BANK ENTRY

  A reply that identifies one or more bank entries is an
  affirmative act and populates the slot directly, verbatim and
  unannotated. No confirmation step, no narrowing step.

  Three forms count as identification:

    verbatim    the entry text, exactly or near-exactly
    positional  "the third one", "the last one", counted against
                the rendered order, which is fixed and
                alphabetical
    referential the entry named unambiguously in other words

  Multiple entries identified → all populate, joined as given.

  Under-resolution is accepted. A bank entry is a valid answer at
  the level Goal operates. Granularity beyond this belongs to
  later stages, not to additional questions here.

  LIMITS

  Silence, "skip", or no reply does not identify an entry and
  never populates. → open:.

  A referential reply that maps to more than one entry, or to
  none, is not identification. Ask once which entry was meant.
  This clarifies the reply; it does not refine the answer, and it
  is the only additional question this clause permits.

  Anything else is an own-words answer and populates as given.
```

The `LIMITS` block is the addition. Without a bound, *referential* is an interpretation licence — the system deciding which entry a user probably meant, which is the plausibility judgment the no-new-noun rule exists to keep out of the highest-leverage slot. The bound keeps the mechanical property: either the reference resolves to exactly one entry or it isn't a reference. The one permitted re-ask is a read of the reply, not a refinement of the answer, so it doesn't reopen the budget concern. Delete the paragraph if you'd rather referential replies be handled loosely.

## Knock-ons

| Location | Change |
|---|---|
| G.3a | unchanged — non-indexed render, own-words line stays primary |
| §6 | `selectable option sets` → `selectable option sets (indexed render; presentation only — selection of an unindexed entry is valid, G.3b)` |
| §7 | drop `entries never indexed` and `index/ordinal replies rejected`; add `entries generic enough that selection under-resolves rather than misresolves` |
| §5 | unaffected |
| §2, §3, §8 | unaffected |

§7 is the one place the argument genuinely changes. The echo-control list previously leaned on ordinal rejection to hold echo rate down; that's gone, so echo will run higher than the earlier draft assumed. It remains the case that no bank entry is a complete answer, so an echoed value is coarse rather than wrong — which is exactly the trade you've accepted, and the note should say so rather than imply the structural safeguards suppress echo as much as they did.

## State

Everything closes. Slot partition and four definition slots confirmed; D.9a with completion test and no-new-noun; differentiated rendering with `open:` bounded at four; `[N/A]` capped at twelve words; fixed banks, vertical unindexed render; echo threshold dropped with changelog note; G.3b as above.

Nothing outstanding except your call on the `LIMITS` paragraph, which doesn't block.

Say go and I'll produce it as a diff against the live spec text — that'll be more useful than prose at this point, and it'll surface any numbering collisions with the current §D and §G.
